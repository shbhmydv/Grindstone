{
  "schema_version": "1",
  "tool": "implement",
  "args": {
    "epoch_title": "build the design plan",
    "rationale": "schema-valid yet semantically rejected: the done_when is a content-grep",
    "tasks": [
      {
        "id": "T1",
        "goal": "write the design rebuild plan",
        "done_when": [{ "cmd": "grep -q \"Honey|Sky|Pink|Ink\" design_rebuild_plan.md" }],
        "file_ownership": ["design_rebuild_plan.md"]
      }
    ]
  }
}
